<section class="finalcta">
  <img class="finalcta__bg" src="/assets/cta-bg.jpg" alt="" aria-hidden="true" loading="lazy" />
  <div class="finalcta__scrim"></div>
  <div class="wrap narrow reveal finalcta__inner">
    <h2>Lift. Let IronPal do the logging.</h2>
    <form class="capture capture--center" id="reserve2" novalidate>
      <div class="capture__row">
        <input type="email" id="email2" name="email" inputmode="email" autocomplete="email" placeholder="you@email.com" aria-label="Email address" required />
        <button type="submit" class="btn">Reserve Early-Bird Access</button>
      </div>
      <p class="capture__hint" id="formHint2">Up to ~50% off · first 200 spots · 48-hour head start.</p>
    </form>
  </div>
</section>

<style>
  .finalcta { position: relative; overflow: hidden; padding-block: clamp(80px,12vw,150px); text-align: center; }
  .finalcta__bg { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; object-position: 50% 40%; z-index: 0; }
  .finalcta__scrim { position: absolute; inset: 0; z-index: 1; background: linear-gradient(0deg, rgba(16,16,30,.92), rgba(16,16,30,.7)); }
  .finalcta__inner { position: relative; z-index: 2; }
  .finalcta h2 { font-size: clamp(28px,5vw,54px); margin-bottom: 30px; }
  @media (max-width: 540px) { .capture__row { flex-direction: column; } .capture :global(.btn) { width: 100%; } }
</style>
